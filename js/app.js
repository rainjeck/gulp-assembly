/*---------------------------------------
    VENDOR.JS

    * jQuery
---------------------------------------*/

jQuery(document).ready(function($) {
// ready start
console.log('Document ready');

/**
 * Module
 */
(function () {
	var module = {
		init: function () {

			console.log('Module ready');

		} // init

	};

	module.init();
}());

// ready end
});